#ifndef UTILITY_HPP
#define UTILITY_HPP

#include <string>

void readName(std::istringstream& stream, std::string& nsme);
bool isCorrectNumber(const std::string& name);
bool isCorrectMarkName(const std::string& name);

#endif
